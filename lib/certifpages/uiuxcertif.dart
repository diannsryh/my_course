import 'package:flutter/material.dart';

class UiuxCertif extends StatelessWidget {
  const UiuxCertif({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
